function heapSort() {
    const parentDiv = document.getElementById("parent");
    const bars = Array.from(parentDiv.childNodes);
    const barHeights = bars.map(bar => parseInt(bar.style.height.slice(0, -2)))
}

function sort(bars, barHeights) {
    
}